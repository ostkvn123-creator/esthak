name: Debug CI

on: push

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Step 1
        run: echo "Start"

      - name: Step 2 (this will fail)
        run: abc123   # ❌ invalid command
